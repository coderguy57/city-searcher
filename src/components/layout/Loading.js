import React from "react";
import icon from "./loading.svg";

const Loading = () => {
  return <img src={icon} alt="Loading" style={{  width: "200px", padding: "3rem", margin: "auto", display: "block" }} />;
};

export default Loading;
