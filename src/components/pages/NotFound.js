import React, { Fragment } from "react";

const NotFound = () => (
  <Fragment>
    <h2 className="text-large">Page Not Found</h2>
    <p>We couldn't find what you were looking for.</p>
  </Fragment>
);

export default NotFound;
