import React, { Fragment } from "react";

const About = () => (
  <Fragment>
    <h2 className="text-large">About City Search</h2>
    <p>
      This is an application by CityPop where the user can search for
      information about cities.
    </p>
  </Fragment>
);

export default About;

